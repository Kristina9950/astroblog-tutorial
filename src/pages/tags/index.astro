---
import BaseLayout from "../../layouts/BaseLayout.astro";

const allPosts = Object.values(
    import.meta.glob("../posts/*.md", { eager: true }),
);
const tags = [
    ...new Set(allPosts.map((post: any) => post.frontmatter.tags).flat()),
];
const pageTitle = "Tag Page";
---

<BaseLayout pageTitle={pageTitle}>
    <style>
        a {
            color: #000000;
        }

        .tags {
            display: flex;
            flex-wrap: wrap;
        }

        .tag {
            margin: 0.25em;
            border: dotted 1px #0e0e0e;
            border-radius: 0.5em;
            padding: 0.5em 1em;
            font-size: 1.15em;
            background-color: #6bf34d;
        }
    </style>
    <div class="tags">
        {
            tags.map((tag) => (
                <p class="tag">
                    <a href={`/tags/${tag}`}>{tag}</a>
                </p>
            ))
        }
    </div>
</BaseLayout>

<!-- Here we link the generated pages to it’s tags -->